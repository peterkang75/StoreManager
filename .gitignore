node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment & secrets
.env
.env.*
!.env.example
.env.backup

# DB dumps
*.dump
snapshot_*
rowcount-*.txt

# Uploads (local Multer store — runtime data, not source)
uploads/

# Local scratch
.local/
